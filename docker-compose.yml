services:
  backend:
    build: ./backend
    container_name: tableorder-backend
    ports:
      - "8080:8080"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: tableorder-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
